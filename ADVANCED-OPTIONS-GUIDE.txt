═══════════════════════════════════════════════════════════════
  ⚡ HƯỚNG DẪN NHANH - ADVANCED OPTIONS ⚡
═══════════════════════════════════════════════════════════════

📋 MỤC LỤC:
-----------
1. Tại sao nên dùng Advanced Options?
2. Cách tìm CSS Selectors
3. Ví dụ cụ thể với các website phổ biến
4. Troubleshooting


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ TẠI SAO NÊN DÙNG ADVANCED OPTIONS?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   KHÔNG dùng Advanced Options:
   ⏱️ Thời gian: ~5-7 giây
   🔍 Tool phải thử 10+ selectors khác nhau
   ⚠️ Có thể click nhầm element
   
   CÓ dùng Advanced Options:
   ⚡ Thời gian: ~2-3 giây (NHANH HƠN 50%!)
   🎯 Chính xác 100%
   ✅ Bỏ qua việc dò tìm

   → KHUYẾN NGHỊ: NÊN DÙNG nếu bạn chạy cùng 1 website nhiều lần!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2️⃣ CÁCH TÌM CSS SELECTORS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CÁCH 1: Dùng Browser DevTools (THƯỜNG DÙNG NHẤT)
-------------------------------------------------

   1. Mở website bạn muốn automation
   2. Nhấn F12 (hoặc Ctrl+Shift+I)
   3. Click vào icon 🔍 "Inspect Element" (góc trên bên trái)
   4. Di chuột và click vào element bạn muốn (search box hoặc result)
   5. Trong DevTools, bạn sẽ thấy HTML của element đó
   6. Copy selector:
   
      CÁCH A: Right-click vào element → Copy → Copy selector
      CÁCH B: Tự viết selector từ attributes:
              - id → #id-name
              - class → .class-name
              - name → [name="value"]


CÁCH 2: Dùng Console
---------------------

   1. Mở Console (F12 → Tab Console)
   2. Gõ lệnh để test selector:
   
      // Test search selector
      document.querySelector('input#search')
      
      // Test result selector
      document.querySelectorAll('a[href*="watch"]')
      
   3. Nếu trả về element → selector đúng!
      Nếu trả về null → selector sai


CÁCH 3: Xem Source Code
------------------------

   1. Right-click vào element → "View Page Source"
   2. Nhìn vào HTML để tìm:
      - id="..."
      - class="..."
      - name="..."


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3️⃣ VÍ DỤ CỤ THỂ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📺 YOUTUBE
----------

URL: https://www.youtube.com/
Từ khóa: "lofi hip hop"

Advanced Options:
┌────────────────────────────────────────────┐
│ Search Selector: input#search              │
│ Result Selector: a#video-title             │
│ Result Index: 0                            │
│ Skip Refresh: ☑️                           │
└────────────────────────────────────────────┘

Giải thích:
- input#search: Ô tìm kiếm YouTube có id="search"
- a#video-title: Link video có id="video-title"
- Result Index 0: Click vào video đầu tiên
- Skip Refresh: Bật (YouTube không cần F5)

⏱️ Thời gian: ~2.5 giây


🔍 GOOGLE
---------

URL: https://www.google.com/
Từ khóa: "puppeteer tutorial"

Advanced Options:
┌────────────────────────────────────────────┐
│ Search Selector: input[name="q"]           │
│ Result Selector: div.g a h3                │
│ Result Index: 0                            │
│ Skip Refresh: ☑️                           │
└────────────────────────────────────────────┘

Giải thích:
- input[name="q"]: Ô search Google có name="q"
- div.g a h3: Heading của kết quả search
- Skip Refresh: Bật (Google không cần F5)

⏱️ Thời gian: ~2 giây


🛒 E-COMMERCE (Generic)
-----------------------

URL: https://example-shop.com/
Từ khóa: "product name"

Advanced Options:
┌────────────────────────────────────────────┐
│ Search Selector: input.search-input        │
│ Result Selector: .product-card a           │
│ Result Index: 0                            │
│ Skip Refresh: ☐                            │
└────────────────────────────────────────────┘

Giải thích:
- input.search-input: Ô search có class="search-input"
- .product-card a: Link trong product card
- Skip Refresh: Tắt (có thể cần F5 để load sản phẩm mới)

⏱️ Thời gian: ~4 giây


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4️⃣ CÁC OPTION GIẢI THÍCH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 SEARCH SELECTOR
------------------

Công dụng: CSS selector của ô tìm kiếm

Ví dụ phổ biến:
   input#search             → Element có id="search"
   input[name="q"]          → Element có name="q"
   input.search-box         → Element có class="search-box"
   input[type="search"]     → Input type search
   #search-input            → Element có id="search-input"

Tip: Thường là <input> tag!


🎯 RESULT SELECTOR
------------------

Công dụng: CSS selector của kết quả tìm kiếm

Ví dụ phổ biến:
   a#video-title            → Link có id="video-title"
   .result a                → Link trong element class="result"
   div.g a                  → Link trong div class="g"
   a[href*="watch"]         → Link có href chứa "watch"
   .search-result a h3      → Heading trong search result

Tip: Thường là <a> tag (link)!


🔢 RESULT INDEX
---------------

Công dụng: Vị trí của kết quả muốn click

Giá trị:
   0 → Kết quả đầu tiên (mặc định)
   1 → Kết quả thứ 2
   2 → Kết quả thứ 3
   ...

Khi nào dùng:
   ✅ Kết quả đầu tiên là quảng cáo → Dùng index 1 hoặc 2
   ✅ Muốn chọn kết quả cụ thể → Đặt index tương ứng


⚡ SKIP REFRESH
--------------

Công dụng: Bỏ qua bước F5 để tăng tốc

NÊN BẬT khi:
   ✅ Website không cần refresh
   ✅ Nội dung đã sẵn sàng
   ✅ Chạy nhiều lần liên tiếp

KHÔNG NÊN BẬT khi:
   ❌ Website cần refresh để load dữ liệu mới
   ❌ Có cache cần clear
   ❌ Nội dung động thay đổi

Tiết kiệm: ~2-3 giây khi bật!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5️⃣ TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ Lỗi: "Không tìm thấy search box"
-----------------------------------

Nguyên nhân:
   - Search selector sai
   - Element chưa load xong

Giải pháp:
   1. Kiểm tra lại selector bằng DevTools
   2. Test selector trong Console
   3. Thử để trống selector (để tool tự tìm)
   4. Tắt Skip Refresh (đợi lâu hơn)


❌ Lỗi: "Không tìm thấy kết quả"
-------------------------------

Nguyên nhân:
   - Result selector sai
   - Kết quả chưa load xong
   - Không có kết quả nào match

Giải pháp:
   1. Kiểm tra lại selector bằng DevTools
   2. Đợi lâu hơn sau khi search (tắt Skip Refresh)
   3. Thử tăng Result Index (có thể đầu tiên là quảng cáo)
   4. Để trống selector (để tool tự tìm)


❌ Click nhầm element
---------------------

Nguyên nhân:
   - Result selector quá chung chung
   - Match nhiều elements không mong muốn

Giải pháp:
   1. Làm selector cụ thể hơn
   2. Ví dụ: a → a.video-title
   3. Hoặc: a → a[href*="specific-word"]


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 TIPS & TRICKS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Selector càng CỤ THỂ càng TỐT
   ❌ BAD:  input
   ✅ GOOD: input#search

2. Test selector TRƯỚC KHI dùng
   → Dùng Console: document.querySelector('...')
   → Phải return element, không phải null

3. LƯU LẠI selectors cho các website thường dùng
   → Tạo file note riêng
   → Copy/paste nhanh lần sau

4. Nếu KHÔNG chắc, ĐỂ TRỐNG
   → Tool vẫn chạy
   → Chậm hơn một chút nhưng CHẮC CHẮN hơn

5. Combine với Skip Refresh để tối đa tốc độ
   ✅ Custom selectors
   ✅ Skip Refresh
   
   → Nhanh nhất có thể! < 3 giây ⚡


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 XEM THÊM
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📄 OPTIMIZATION-GUIDE.md - Hướng dẫn tối ưu chi tiết
📄 README.md             - Tổng quan dự án
📄 QUICK-GUIDE.txt       - Hướng dẫn cơ bản


═══════════════════════════════════════════════════════════════
Made with ⚡ by Maple Team | v2.0.0
═══════════════════════════════════════════════════════════════
